apply plugin: 'application'
apply plugin: 'com.github.johnrengelman.shadow'

mainClassName = 'com.mattrjacobs.hystrix.client.rxnetty.StartClient'

dependencies {
	compile 'io.reactivex:rxnetty:0.4.8'
	compile project(path: ':core')
}